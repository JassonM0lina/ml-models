# =============================================================================
# SageMaker sklearn 1.2-1 Container Pre-installed Packages
# =============================================================================
# These packages are already installed in the SageMaker sklearn container.
# DO NOT add them to requirements.txt - they will cause conflicts.
#
# Pre-installed versions (from SageMaker sklearn 1.2-1 container):
#   - scikit-learn==1.2.1
#   - numpy==1.24.1
#   - scipy==1.5.3
#   - pandas==1.1.3
#
# Reference: https://github.com/aws/sagemaker-scikit-learn-container
# =============================================================================
#
# Additional packages for Saudi Crude Burn Forecaster
# (pandas, numpy, scikit-learn already included in base container)

# XGBoost 1.7.6 is compatible with numpy 1.24.1 in the container
# XGBoost 2.0+ requires numpy 1.25+ which conflicts with the container
xgboost==1.7.6
matplotlib>=3.7.0
seaborn>=0.12.0
requests>=2.31.0
urllib3>=2.0.0
python-dateutil>=2.8.0

# Note: Windows-specific packages (pywin32, win32com) removed for Linux SageMaker environment
